# Generated by Composer — never commit.
vendor/
composer.lock

# Generated by npm — never commit.
node_modules/
package-lock.json

# Tool caches.
.phpcs.cache
.phpunit.result.cache
.phpstan.cache

# composer coverage output — reproducible, and embeds this machine's own
# absolute filesystem paths; never commit (audit H-1, AUDIT-0.9.39.md §6).
coverage/

# wp-env runtime data.
.wp-env/
.wp-env.override.json

# WP-CLI phar — downloaded on demand by bin/make-pot.sh.
wp-cli.phar


# Playwright artefacts.
e2e/.auth/
test-results/
playwright-report/
